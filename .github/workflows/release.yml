name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            artifact: bento-linux-x64
            ext: ""
          - os: macos-latest
            target: aarch64-apple-darwin
            artifact: bento-macos-arm64
            ext: ""
          - os: macos-13
            target: x86_64-apple-darwin
            artifact: bento-macos-x64
            ext: ""
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            artifact: bento-windows-x64
            ext: ".exe"

    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Install Linux dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libgtk-3-dev libayatana-appindicator3-dev librsvg2-dev

      - name: Build CLI
        run: cargo build --release --target ${{ matrix.target }} -p bento-cli

      - name: Build Supervisor
        run: cargo build --release --target ${{ matrix.target }} -p bento-supervisor

      - name: Package artifacts
        shell: bash
        run: |
          mkdir -p dist
          # Binary name uses hyphens on most platforms but may use underscores
          for name in bento bento_cli; do
            src="target/${{ matrix.target }}/release/${name}${{ matrix.ext }}"
            [ -f "$src" ] && cp "$src" "dist/bento${{ matrix.ext }}" && break
          done
          for name in bento-supervisor bento_supervisor; do
            src="target/${{ matrix.target }}/release/${name}${{ matrix.ext }}"
            [ -f "$src" ] && cp "$src" "dist/bento-supervisor${{ matrix.ext }}" && break
          done
          cd dist
          if [ "${{ matrix.ext }}" = ".exe" ]; then
            7z a ../${{ matrix.artifact }}.zip *
          else
            chmod +x bento bento-supervisor
            tar czf ../${{ matrix.artifact }}.tar.gz *
          fi

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: |
            ${{ matrix.artifact }}.zip
            ${{ matrix.artifact }}.tar.gz

  release:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          merge-multiple: true

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: |
            bento-linux-x64.tar.gz
            bento-macos-arm64.tar.gz
            bento-macos-x64.tar.gz
            bento-windows-x64.zip
